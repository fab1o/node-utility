/**
 * @access public
 * @since 1.0.0
 * @typedef {Object} object - util.object
 * @property {Function} toString - Converts objects to a string value.
 */
const toString = require('./toString');

module.exports = {
    toString
};
