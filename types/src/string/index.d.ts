export { replace };
/**
 * - string
 */
export type string = {
    /**
     * - Replaces all entries of `template` with given `data`.
     */
    replace: Function;
};
import replace = require("./replace");
